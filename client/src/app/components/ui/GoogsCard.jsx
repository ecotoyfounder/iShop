import React from "react";
import PropTypes from "prop-types";

const GoodsCard = ({good}) => {
    return (
        <div className="container bg-bgLight">
            1
        </div>
    );
};

GoodsCard.propTypes = {
    good: PropTypes.object
};

export default GoodsCard;
